import test from 'node:test';import assert from 'node:assert/strict';import ts from 'typescript';import fs from 'node:fs';
async function load(path){const source=ts.transpileModule(fs.readFileSync(path,'utf8'),{compilerOptions:{module:ts.ModuleKind.ESNext}}).outputText;return import('data:text/javascript;base64,'+Buffer.from(source).toString('base64'));}
test('Area, RT and Area-logo filters keep national logos and distinguish areas',async()=>{const {matchesLogo,originals,logoScope,groupedLogos}=await load('lib/catalog.ts');assert.ok(matchesLogo(originals[0],'1','400','All'));assert.equal(matchesLogo(originals[1],'1','All','Area'),false);assert.ok(matchesLogo(originals[1],'18','All','Area'));assert.ok(matchesLogo({area:18,rt:400,category:'Table'},'18','400','Table'));assert.equal(matchesLogo({area:18,rt:400,category:'Table'},'18','1','Table'),false);assert.equal(logoScope(originals[0]),'Official');assert.equal(logoScope(originals[1]),'Area · Area 18');assert.equal(logoScope({category:'Table',area:7,rt:12}),'Table · Area 7 · RT 12');assert.deepEqual(groupedLogos(originals).map(g=>g.category),['Official','Area']);});
test('Upload metadata generates RT/Area labels and validates boundaries',async()=>{const {logoFields}=await load('lib/validation.ts');const f=new FormData();f.set('category','Table');f.set('area','18');f.set('rt','400');assert.deepEqual(logoFields(f,false),{name:'RT 400',category:'Table',area:18,rt:400});f.set('rt','401');assert.throws(()=>logoFields(f,false),/RT/);f.set('category','Area');assert.equal(logoFields(f,false).name,'Area 18');f.set('area','0');assert.throws(()=>logoFields(f,false),/Area/);});
